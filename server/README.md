# PetHome — Server API (embedded in client for Vercel)

This folder is a copy of the standalone server, bundled inside the client repo so the full stack deploys on **one Vercel project** at `/api/...`.

For assignment submission, the **standalone server repo** is:

[pet-adoption-server](https://github.com/Rumman954/pet-adoption-server)

See `../README.md` (client root) for deploy instructions and `../../server/README.md` for full API documentation.
